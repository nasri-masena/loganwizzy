services:
  - type: web
    name: coins-finder
    env: python
    plan: free
    instanceCount: 1
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python3 main.py"
    healthCheckPath: "/"
    autoDeploy: true
